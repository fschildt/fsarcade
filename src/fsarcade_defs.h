#ifndef FSARCADE_DEFS_H
#define FSARCADE_DEFS_H


#include <cstdint>
#include <cstddef>

#define MEBIBYTES(x) (1024*1024*x)
#define ARRAY_COUNT(array) (sizeof(array) / sizeof(array[0]))


#endif // FSARCADE_DEFS_H
